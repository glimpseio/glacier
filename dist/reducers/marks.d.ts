import { MarkState } from "../model";
import { AllActions } from "../actions";
/**
 * marks member reducer - handles actions and state related to mark, description, and size management
 */
export declare function marks(state: MarkState | undefined, action: AllActions): MarkState;

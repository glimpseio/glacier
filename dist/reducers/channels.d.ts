import { ChannelState } from "../model";
import { AllActions } from "../actions";
/**
 * channels member reducer - handles actions and state related to channel management
 */
export declare function channels(state: ChannelState | undefined, action: AllActions): ChannelState;
